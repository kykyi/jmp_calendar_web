<div class="w-full mx-4 mt-28 bg-white shadow-lg rounded-lg p-6 md:p-8">
  <h2 class="text-center text-3xl md:text-4xl font-bold text-gray-900 mb-6 md:mb-8">Get Your Timetable</h2>
  <p>Generate an ics file which you can import into any calendar app. Note that only events in the future and for the specified .xlsx file are generated!</p>
  <%= form_with url: calendars_path, method: :post, local: true, data: { turbo: false }, class: "space-y-4 md:space-y-6" do |form| %>

    <div class="flex flex-col md:flex-row md:space-x-4">
      <div class="flex-1">
        <%= form.label :pbl, 'PBL', class: "block text-md md:text-lg font-medium text-gray-700" %>
        <%= form.select :pbl, ('A'..'Q').to_a.map { |letter| [letter, letter] }, { prompt: 'Select PBL' }, class: "mt-1 block w-full py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 text-base md:text-md" %>
      </div>

      <div class="flex-1">
        <%= form.label :clin, 'CLIN', class: "block text-md md:text-lg font-medium text-gray-700" %>
        <%= form.select :clin, (1..20).to_a.map { |number| [number, number] }, { prompt: 'Select CLIN' }, class: "mt-1 block w-full py-2 px-3 border border-gray-300 bg-white rounded-md shadow-sm focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 text-base md:text-md" %>
      </div>
    </div>


    <div class="flex justify-center">
      <%= form.submit "Submit", class: "mt-3 w-full md:w-auto px-6 py-2 border border-transparent shadow-sm text-base md:text-md font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
    </div>

    <div class="flex items-center">

      <a href="mailto:c3438165@uon.edu.au?subject=I need help with my timetable" class="ml-auto text-blue-600 visited:text-purple-600">
        Need help?
      </a>
    </div>

  <% end %>
</div>
